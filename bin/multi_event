#!/bin/sh

SF=/dev/shm/multi_event_settings

source $SF

if [ -z $1 ]; then
	echo $MULTI_EVENT
else
	echo $1 | grep -q [0-9]*,[0-9]*,[0-9]*
	if [ $? -eq 0 ]; then
		echo "MULTI_EVENT=$1" > /dev/shm/multi_event_settings
	else
		echo "ERROR INVALID DATA"
		exit 1
	fi
fi

exit 0



